- name: variable using prompt
  hosts: node

  vars_prompt:
   - name: username
     prompt: "enter your name"
     private: false

   - name: password
     prompt: "enter you password"
     private: true

  tasks:
    - name: print entered value
      ansible.builtin.debug:
        msg: "hi {{username}}, your {{password}}"